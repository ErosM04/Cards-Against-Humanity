import 'package:cards_against_humanity/features/load/domain/entities/answer_list.dart';

/// The model is only here for the data layer, so it won't be used outside it.
class AnswerListModel extends AnswerList {
  const AnswerListModel({required super.list});
}
