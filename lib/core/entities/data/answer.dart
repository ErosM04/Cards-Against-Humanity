import 'package:cards_against_humanity/core/entities/data/card.dart';

/// Stores the informations of a Cards Against Humanity's answer card.
class Answer extends CardAH {
  const Answer({
    required super.id,
    required super.text,
  });
}
